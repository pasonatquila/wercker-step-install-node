name: install-node
version: 2.1.2
summary: Install Node.js & NPM in the current pipeline

keywords:
  - install
  - node
  - npm

properties:
  - name: version
    type: string
    required: false
    default: 6.9.4

