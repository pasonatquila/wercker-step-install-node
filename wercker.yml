box: wercker/default

dev:
  box: debian:jessie
  steps:
    - install-packages:
        packages: wget

    - shellcheck

    - script:
        name: Execute run.sh script
        code: |
          sh run.sh

    - internal/shell:
        cmd: bash

build:
  steps:
    - shellcheck
